# ecg_cpp_project
This is a C++ application designed for processing and analyzing ECG data. It takes raw biometric readings from an input dataset, processes the information to detect arrhythmias using its core source file (ecg_arrhythmia.cpp), and outputs the diagnostic or analysis results into a structured results file (results.csv).
